# systemd unit — install to /etc/systemd/system/obsidian-vault-mcp.service
#
#   sudo cp deploy/obsidian-vault-mcp.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now obsidian-vault-mcp
#   journalctl -u obsidian-vault-mcp -f
#
# Secrets live in /etc/obsidian-vault-mcp.env (chmod 600, root-owned) — see README.

[Unit]
Description=Obsidian Vault MCP server
After=network.target

[Service]
Type=simple
# A dedicated, unprivileged user that can read the vault:
User=vaultmcp
Group=vaultmcp
WorkingDirectory=/opt/obsidian-vault-mcp
ExecStart=/usr/bin/dotnet /opt/obsidian-vault-mcp/PersonalMcpVault.dll
EnvironmentFile=/etc/obsidian-vault-mcp.env
Environment=ASPNETCORE_URLS=http://127.0.0.1:5090
Environment=DOTNET_CLI_TELEMETRY_OPTOUT=1
Restart=on-failure
RestartSec=5

# Hardening
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=read-only
PrivateTmp=true
# Allow writing only the SQLite store dir (adjust to your Auth__StorePath):
ReadWritePaths=/opt/obsidian-vault-mcp

[Install]
WantedBy=multi-user.target
